let menu_button, menu_icon, container, main, map, map_scale, mapWidth, mapHeight, moving, pointer, p_avatar, p_shape;

randInt = function(min, max) {
    return Math.floor(Math.random() * (max - min + 1) ) + min;
}